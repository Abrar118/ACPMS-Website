# ACPSCM Dark Glass UI Revamp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform all public-facing pages to a dark glass aesthetic with crimson red accents, inspired by Linear/Raycast, with ambient glow effects and Aceternity UI animations.

**Architecture:** Dark-first design using CSS custom properties. Glass cards via `backdrop-blur-xl` + low-opacity borders. Ambient red glow via radial gradients. Inter font for clean typography. Framer Motion for entrance animations. Each page component is rebuilt in-place — no new routing.

**Tech Stack:** Tailwind CSS 4, shadcn/ui, Framer Motion, Inter font (Google Fonts), Lucide icons

**Design Reference:** Deep black background (#0a0a0b), glass surfaces (rgba white 3-6%), crimson accent (#dc2626), ambient red glow, rounded-2xl corners, generous whitespace.

---

## File Structure

### Files to modify:
- `app/globals.css` — Dark-first color tokens, glass utilities, glow effects, Inter font
- `app/layout.tsx` — Switch to Inter font, force dark theme
- `app/page.tsx` — Landing page composition
- `components/home/HeroSection.tsx` — Full rewrite: dark glass hero with gradient mesh background
- `components/home/WhatWeDo.tsx` — Rewrite: glass cards with crimson accents
- `components/home/WhoWeAre.tsx` — Rewrite: bento grid with glass surfaces
- `components/home/ClubHighlights.tsx` — Rewrite: glass highlight cards with glow
- `components/home/ClientTestimonials.tsx` — Rewrite: glass testimonial card
- `components/home/Footer.tsx` — Rewrite: minimal dark footer
- `components/shared/Navbar.tsx` — Rewrite: glass navbar with backdrop blur
- `components/events/EventsClient.tsx` — Glass card grid for events
- `components/events/EventDetailClient.tsx` — Glass detail layout
- `app/about/page.tsx` — Glass member cards
- `app/magazine/page.tsx` — Glass magazine grid
- `app/resources/page.tsx` — Glass resources layout

### New files to create:
- `components/ui/ambient-glow.tsx` — Reusable red ambient glow background component
- `components/ui/glass-card.tsx` — Reusable glass card component
- `components/ui/section-header.tsx` — Consistent section heading with subtitle

---

### Task 1: Design System Foundation

**Files:**
- Modify: `app/globals.css`
- Modify: `app/layout.tsx`
- Create: `components/ui/ambient-glow.tsx`
- Create: `components/ui/glass-card.tsx`
- Create: `components/ui/section-header.tsx`

- [ ] **Step 1: Update `app/globals.css` with dark-first design tokens**

Replace the entire `globals.css` with the new dark glass design system. Key changes:
- Switch font import from Montserrat to Inter
- Dark-first color scheme: deep black backgrounds, glass surfaces, crimson accent
- Add glass utility classes
- Add ambient glow keyframes
- Keep existing Tailwind v4 `@theme inline` pattern

```css
@import url("https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap");
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

:root {
    --background: #0a0a0b;
    --foreground: #f5f5f5;
    --card: rgba(255, 255, 255, 0.03);
    --card-foreground: #f5f5f5;
    --popover: #0f0f10;
    --popover-foreground: #f5f5f5;
    --primary: #dc2626;
    --primary-foreground: #ffffff;
    --secondary: rgba(255, 255, 255, 0.06);
    --secondary-foreground: #f5f5f5;
    --muted: rgba(255, 255, 255, 0.04);
    --muted-foreground: #737373;
    --accent: #dc2626;
    --accent-foreground: #ffffff;
    --destructive: #ef4444;
    --destructive-foreground: #ffffff;
    --border: rgba(255, 255, 255, 0.08);
    --input: rgba(255, 255, 255, 0.08);
    --ring: #dc2626;
    --chart-1: #dc2626;
    --chart-2: #b91c1c;
    --chart-3: #ef4444;
    --chart-4: #f87171;
    --chart-5: #fca5a5;
    --sidebar: #080808;
    --sidebar-foreground: #f5f5f5;
    --sidebar-primary: #dc2626;
    --sidebar-primary-foreground: #ffffff;
    --sidebar-accent: rgba(255, 255, 255, 0.06);
    --sidebar-accent-foreground: #f5f5f5;
    --sidebar-border: rgba(255, 255, 255, 0.08);
    --sidebar-ring: #dc2626;
    --font-sans: "Inter", ui-sans-serif, system-ui, -apple-system, sans-serif;
    --font-serif: ui-serif, Georgia, Cambria, "Times New Roman", Times, serif;
    --font-mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    --radius: 0.75rem;
    --shadow-2xs: 0 1px 3px 0px rgba(0, 0, 0, 0.3);
    --shadow-xs: 0 1px 3px 0px rgba(0, 0, 0, 0.3);
    --shadow-sm: 0 1px 3px 0px rgba(0, 0, 0, 0.4), 0 1px 2px -1px rgba(0, 0, 0, 0.3);
    --shadow: 0 1px 3px 0px rgba(0, 0, 0, 0.4), 0 1px 2px -1px rgba(0, 0, 0, 0.3);
    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.4), 0 2px 4px -2px rgba(0, 0, 0, 0.3);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.4), 0 4px 6px -4px rgba(0, 0, 0, 0.3);
    --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.5), 0 8px 10px -6px rgba(0, 0, 0, 0.3);
    --shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.6);
    --tracking-normal: 0em;
    --spacing: 0.25rem;
}

.dark {
    /* Dark mode uses the same tokens — this is a dark-first design */
}

@theme inline {
    --color-background: var(--background);
    --color-foreground: var(--foreground);
    --color-card: var(--card);
    --color-card-foreground: var(--card-foreground);
    --color-popover: var(--popover);
    --color-popover-foreground: var(--popover-foreground);
    --color-primary: var(--primary);
    --color-primary-foreground: var(--primary-foreground);
    --color-secondary: var(--secondary);
    --color-secondary-foreground: var(--secondary-foreground);
    --color-muted: var(--muted);
    --color-muted-foreground: var(--muted-foreground);
    --color-accent: var(--accent);
    --color-accent-foreground: var(--accent-foreground);
    --color-destructive: var(--destructive);
    --color-destructive-foreground: var(--destructive-foreground);
    --color-border: var(--border);
    --color-input: var(--input);
    --color-ring: var(--ring);
    --color-chart-1: var(--chart-1);
    --color-chart-2: var(--chart-2);
    --color-chart-3: var(--chart-3);
    --color-chart-4: var(--chart-4);
    --color-chart-5: var(--chart-5);
    --color-sidebar: var(--sidebar);
    --color-sidebar-foreground: var(--sidebar-foreground);
    --color-sidebar-primary: var(--sidebar-primary);
    --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
    --color-sidebar-accent: var(--sidebar-accent);
    --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
    --color-sidebar-border: var(--sidebar-border);
    --color-sidebar-ring: var(--sidebar-ring);

    --font-sans: var(--font-sans);
    --font-mono: var(--font-mono);
    --font-serif: var(--font-serif);

    --radius-sm: calc(var(--radius) - 4px);
    --radius-md: calc(var(--radius) - 2px);
    --radius-lg: var(--radius);
    --radius-xl: calc(var(--radius) + 4px);

    --shadow-2xs: var(--shadow-2xs);
    --shadow-xs: var(--shadow-xs);
    --shadow-sm: var(--shadow-sm);
    --shadow: var(--shadow);
    --shadow-md: var(--shadow-md);
    --shadow-lg: var(--shadow-lg);
    --shadow-xl: var(--shadow-xl);
    --shadow-2xl: var(--shadow-2xl);

    --animate-shine: shine var(--duration) infinite linear;
    @keyframes shine {
        0% { background-position: 0% 0%; }
        50% { background-position: 100% 100%; }
        to { background-position: 0% 0%; }
    }
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.1); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(220, 38, 38, 0.5); }

@layer base {
    * {
        @apply border-border outline-ring/50;
    }
    body {
        @apply text-foreground;
        pointer-events: auto !important;
        font-family: "Inter", var(--font-sans);
        background-color: var(--background);
    }
    a[href], button, [role="button"], input[type="button"], input[type="submit"] {
        cursor: pointer;
    }
    button:disabled, input[disabled], [aria-disabled="true"] {
        cursor: default;
    }
}

.line-clamp-1 { overflow: hidden; display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 1; }
.line-clamp-2 { overflow: hidden; display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2; }
.line-clamp-3 { overflow: hidden; display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 3; }

::view-transition-old(root), ::view-transition-new(root) { animation: none; mix-blend-mode: normal; }

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
}

@keyframes glow-pulse {
    0%, 100% { opacity: 0.4; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.05); }
}
```

- [ ] **Step 2: Update `app/layout.tsx` to use Inter font and force dark mode**

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ReactQueryClientProvider } from "@/components/ReactQueryClientProvider";
import { ThemeProvider } from "@/components/theme-provider";
import { Toaster } from "sonner";
import { ConditionalNavbar } from "@/components/ConditionalNavbar";
import { getCurrentUser } from "@/lib/auth-server";

const inter = Inter({
    subsets: ["latin"],
    variable: "--font-inter",
});

export const metadata: Metadata = {
    title: "ACPSCM",
    description: "Adamjee Cantonment Public School Club of Mathematics",
};

export default async function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    const { user, profile } = await getCurrentUser();

    return (
        <ReactQueryClientProvider>
            <html lang="en" className="dark">
                <body className={`${inter.variable} font-sans antialiased`}>
                    <ThemeProvider
                        attribute="class"
                        defaultTheme="dark"
                        forcedTheme="dark"
                        disableTransitionOnChange
                    >
                        <ConditionalNavbar
                            user={user}
                            profile={profile ? JSON.parse(JSON.stringify(profile)) : null}
                        />
                        <main className="flex flex-col min-h-screen">
                            {children}
                        </main>
                        <Toaster
                            richColors
                            position="top-right"
                            toastOptions={{
                                style: {
                                    background: "rgba(15, 15, 16, 0.9)",
                                    border: "1px solid rgba(255, 255, 255, 0.08)",
                                    color: "#f5f5f5",
                                    backdropFilter: "blur(12px)",
                                },
                            }}
                        />
                    </ThemeProvider>
                </body>
            </html>
        </ReactQueryClientProvider>
    );
}
```

- [ ] **Step 3: Create `components/ui/ambient-glow.tsx`**

```tsx
import { cn } from "@/lib/utils";

interface AmbientGlowProps {
  className?: string;
  color?: string;
  size?: "sm" | "md" | "lg";
  position?: "top-left" | "top-right" | "center" | "bottom-left" | "bottom-right";
}

export function AmbientGlow({
  className,
  color = "rgba(220, 38, 38, 0.15)",
  size = "md",
  position = "center",
}: AmbientGlowProps) {
  const sizeClasses = {
    sm: "w-[300px] h-[300px]",
    md: "w-[500px] h-[500px]",
    lg: "w-[800px] h-[800px]",
  };

  const positionClasses = {
    "top-left": "-top-1/4 -left-1/4",
    "top-right": "-top-1/4 -right-1/4",
    center: "top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2",
    "bottom-left": "-bottom-1/4 -left-1/4",
    "bottom-right": "-bottom-1/4 -right-1/4",
  };

  return (
    <div
      className={cn(
        "absolute rounded-full blur-3xl pointer-events-none",
        sizeClasses[size],
        positionClasses[position],
        className
      )}
      style={{
        background: `radial-gradient(circle, ${color} 0%, transparent 70%)`,
        animation: "glow-pulse 8s ease-in-out infinite",
      }}
      aria-hidden="true"
    />
  );
}
```

- [ ] **Step 4: Create `components/ui/glass-card.tsx`**

```tsx
import { cn } from "@/lib/utils";
import { forwardRef } from "react";

interface GlassCardProps extends React.HTMLAttributes<HTMLDivElement> {
  hover?: boolean;
  glow?: boolean;
}

export const GlassCard = forwardRef<HTMLDivElement, GlassCardProps>(
  ({ className, hover = true, glow = false, children, ...props }, ref) => {
    return (
      <div
        ref={ref}
        className={cn(
          "rounded-2xl border border-white/[0.08] bg-white/[0.03] backdrop-blur-xl",
          hover && "transition-all duration-300 hover:border-white/[0.15] hover:bg-white/[0.06]",
          glow && "shadow-[0_0_30px_-5px_rgba(220,38,38,0.15)]",
          className
        )}
        {...props}
      >
        {children}
      </div>
    );
  }
);
GlassCard.displayName = "GlassCard";
```

- [ ] **Step 5: Create `components/ui/section-header.tsx`**

```tsx
"use client";

import { cn } from "@/lib/utils";
import { motion } from "framer-motion";

interface SectionHeaderProps {
  title: string;
  subtitle?: string;
  className?: string;
  align?: "left" | "center";
}

export function SectionHeader({ title, subtitle, className, align = "center" }: SectionHeaderProps) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-100px" }}
      transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
      className={cn(
        "mb-12",
        align === "center" && "text-center",
        className
      )}
    >
      <h2 className="text-3xl md:text-4xl font-bold tracking-tight text-foreground">
        {title}
      </h2>
      {subtitle && (
        <p className="mt-3 text-muted-foreground text-lg max-w-2xl mx-auto">
          {subtitle}
        </p>
      )}
    </motion.div>
  );
}
```

- [ ] **Step 6: Verify the dev server renders the dark theme**

Run:
```bash
npm run dev
```

Open `http://localhost:3000` — the page should now have a deep black background with the Inter font. Existing content will look broken (wrong colors) — that's expected, we're replacing it in subsequent tasks.

- [ ] **Step 7: Commit**

```bash
git add app/globals.css app/layout.tsx components/ui/ambient-glow.tsx components/ui/glass-card.tsx components/ui/section-header.tsx
git commit -m "feat: establish dark glass design system with Inter font and glass primitives"
```

---

### Task 2: Navbar Redesign

**Files:**
- Modify: `components/shared/Navbar.tsx`

- [ ] **Step 1: Rewrite the Navbar as a glass header**

The navbar should be:
- Fixed, transparent on top, gains `backdrop-blur-xl` + glass background on scroll
- Logo on left, nav links center, auth/theme on right
- Crimson accent on active link
- Mobile: glass sheet menu

Read the current `components/shared/Navbar.tsx` (375 lines). Preserve all functionality (auth state, admin link, mobile menu, logout) but restyle:
- Replace the background from solid to `bg-transparent` → `bg-white/[0.03] backdrop-blur-xl border-b border-white/[0.08]` on scroll
- Nav links: `text-muted-foreground hover:text-foreground` with active state using `text-primary` (crimson)
- Sign In button: `border border-white/[0.15] hover:bg-white/[0.06]`
- Join Now button: `bg-primary text-primary-foreground hover:bg-primary/90`
- Mobile sheet: glass background `bg-[#0a0a0b]/95 backdrop-blur-xl`
- Avatar: ring on hover with `ring-primary/50`

The scroll detection already exists in the component — update the class application to use glass styling.

- [ ] **Step 2: Verify desktop and mobile nav**

Run dev server, check:
- Transparent navbar at top of page
- Glass effect appears on scroll
- All nav links work
- Mobile hamburger opens glass sheet
- Login/signup buttons visible and styled
- Admin link appears when logged in

- [ ] **Step 3: Commit**

```bash
git add components/shared/Navbar.tsx
git commit -m "feat: redesign navbar with glass morphism and crimson accents"
```

---

### Task 3: Landing Page Hero

**Files:**
- Modify: `components/home/HeroSection.tsx`

- [ ] **Step 1: Rewrite HeroSection with dark glass aesthetic**

Complete rewrite of the hero section (currently 254 lines). New design:

- Full viewport height (`min-h-screen`)
- Deep black background with subtle gradient mesh
- Two ambient red glow orbs (top-right and bottom-left) using `AmbientGlow` component
- Large heading: "ACPS Club of Mathematics" with gradient text (white to gray)
- Subtitle: "Think Logical" in muted foreground
- Short description paragraph
- Two CTA buttons: "Join Now" (crimson filled) and "Explore Events" (glass outline)
- Floating mathematical symbols with subtle animation (keep the existing ones but make them more subtle — lower opacity, slower)
- Scroll indicator at bottom

Key styling:
- Heading: `text-5xl md:text-7xl font-bold bg-gradient-to-b from-white to-white/60 bg-clip-text text-transparent`
- CTAs: Primary uses `bg-primary`, secondary uses glass card style
- Ambient glow: Two `AmbientGlow` components with `color="rgba(220, 38, 38, 0.12)"`
- Math symbols: `text-white/[0.04]` (very subtle), floating animation

Use Framer Motion for staggered entrance animations on heading, subtitle, and buttons.

- [ ] **Step 2: Verify hero renders correctly**

Check: full-height dark section, red ambient glow visible, text readable, CTAs clickable, math symbols subtle.

- [ ] **Step 3: Commit**

```bash
git add components/home/HeroSection.tsx
git commit -m "feat: redesign hero section with dark glass aesthetic and ambient glow"
```

---

### Task 4: Landing Page Sections

**Files:**
- Modify: `components/home/WhatWeDo.tsx`
- Modify: `components/home/WhoWeAre.tsx`
- Modify: `components/home/ClubHighlights.tsx`
- Modify: `components/home/ClientTestimonials.tsx`

- [ ] **Step 1: Rewrite WhatWeDo (Our Objectives)**

4 glass cards in a responsive grid. Each card:
- `GlassCard` component with icon, title, description
- Icon in a circle with crimson tint: `bg-primary/10 text-primary`
- Hover: card border brightens, subtle lift
- Framer Motion: staggered entrance from below

Use `SectionHeader` with title "Our Objectives" and subtitle.

- [ ] **Step 2: Rewrite WhoWeAre**

Bento grid layout with glass cards. Keep the 8 features but restyle:
- Use `GlassCard` for each item
- Some cards span 2 columns (keep existing spans)
- Icons: `text-primary` on icon containers
- Title and description in card
- Framer Motion staggered entrance

Use `SectionHeader` with title "Where Others Stop, We Keep Going".

- [ ] **Step 3: Rewrite ClubHighlights**

3 glass cards showing latest event, most popular resource, latest magazine:
- `GlassCard` with `glow` prop on the event card (featured)
- Image/thumbnail at top, info below
- Badge for type (Event / Resource / Magazine) with crimson accent
- "Learn More" link styled as subtle text link with arrow

- [ ] **Step 4: Rewrite ClientTestimonials**

Single testimonial in a centered glass card:
- Large quote marks in `text-primary/20`
- Quote text in larger font
- Avatar + name + role below
- `AmbientGlow` behind the card (subtle, centered)

- [ ] **Step 5: Verify all sections**

Scroll through the full landing page. Check:
- Consistent dark glass aesthetic across all sections
- Smooth scroll transitions
- No jarring color changes between sections
- Responsive on mobile (375px)

- [ ] **Step 6: Commit**

```bash
git add components/home/WhatWeDo.tsx components/home/WhoWeAre.tsx components/home/ClubHighlights.tsx components/home/ClientTestimonials.tsx
git commit -m "feat: redesign landing page sections with glass cards and crimson accents"
```

---

### Task 5: Footer Redesign

**Files:**
- Modify: `components/home/Footer.tsx`

- [ ] **Step 1: Rewrite Footer**

Minimal dark footer:
- Top border: `border-t border-white/[0.06]`
- Background: slightly elevated from page `bg-white/[0.02]`
- 4-column grid (same structure): Club branding, Quick Links, Contact, Social
- Links: `text-muted-foreground hover:text-foreground transition-colors`
- Social icons: glass circle buttons
- Copyright: `text-muted-foreground text-sm`
- Update year to 2026

- [ ] **Step 2: Commit**

```bash
git add components/home/Footer.tsx
git commit -m "feat: redesign footer with dark glass aesthetic"
```

---

### Task 6: Events Pages

**Files:**
- Modify: `components/events/EventsClient.tsx`
- Modify: `components/events/EventDetailClient.tsx`
- Modify: `components/events/EventRegistrationDialog.tsx`
- Modify: `app/events/page.tsx`
- Modify: `app/events/[id]/page.tsx`

- [ ] **Step 1: Restyle EventsClient**

The events listing page:
- `SectionHeader` for page title
- Filter tabs: glass pill buttons, active state with `bg-primary text-primary-foreground`
- Event cards: `GlassCard` with poster image, date badge (crimson), title, venue, description
- Search input: glass-styled with `bg-white/[0.04] border-white/[0.08]`
- Pagination: glass pill buttons

- [ ] **Step 2: Restyle EventDetailClient**

Single event detail page:
- Hero section with poster image (full width, gradient overlay from transparent to `#0a0a0b`)
- Event info in glass cards: date, venue, mode, registration deadline
- Description rendered in a glass panel
- Competition list in glass cards with fee badges
- Registration CTA: crimson primary button

- [ ] **Step 3: Restyle EventRegistrationDialog**

Glass-styled dialog:
- Dialog overlay: `bg-black/60 backdrop-blur-sm`
- Dialog content: `bg-[#0f0f10] border border-white/[0.08] rounded-2xl`
- Form inputs: glass-styled
- Submit button: crimson

- [ ] **Step 4: Update page wrappers**

Ensure `app/events/page.tsx` and `app/events/[id]/page.tsx` have proper dark background and padding.

- [ ] **Step 5: Verify events flow**

Browse events list → click event → check detail page → open registration dialog. All should be cohesive dark glass.

- [ ] **Step 6: Commit**

```bash
git add components/events/ app/events/
git commit -m "feat: redesign events pages with dark glass aesthetic"
```

---

### Task 7: About Page

**Files:**
- Modify: `app/about/page.tsx`

- [ ] **Step 1: Restyle About page**

The about page has mission, values, constitution, journey, leadership tabs:
- Tab bar: glass pill buttons with crimson active state
- Mission section: large text in glass panel
- Member cards: `GlassCard` with avatar, name, designation, social links
- Social link icons: `text-muted-foreground hover:text-primary`
- Session/year tabs: glass pills
- `AmbientGlow` behind the leadership section

- [ ] **Step 2: Verify about page**

Check all tabs render, member photos display, social links work.

- [ ] **Step 3: Commit**

```bash
git add app/about/
git commit -m "feat: redesign about page with glass cards and dark theme"
```

---

### Task 8: Magazine Page

**Files:**
- Modify: `app/magazine/page.tsx`

- [ ] **Step 1: Restyle Magazine page**

Magazine grid:
- `SectionHeader` with "Our Publications"
- Filter/search bar: glass-styled
- Magazine cards: `GlassCard` with cover image, title, volume/issue, summary
- Featured tab: cards with `glow` prop
- Download/read buttons: glass outline and crimson primary

- [ ] **Step 2: Commit**

```bash
git add app/magazine/
git commit -m "feat: redesign magazine page with dark glass aesthetic"
```

---

### Task 9: Resources Page

**Files:**
- Modify: `app/resources/page.tsx`
- Modify: `components/resources/ResourcesClient.tsx`

- [ ] **Step 1: Restyle Resources page**

Resources grid:
- Category filter tabs: glass pills with crimson active
- Resource cards: `GlassCard` with type indicator (PDF icon, video icon, link icon)
- Featured resources: crimson border accent via `border-primary/30`
- View count badge
- `AmbientGlow` behind featured section

- [ ] **Step 2: Commit**

```bash
git add app/resources/ components/resources/
git commit -m "feat: redesign resources page with dark glass aesthetic"
```

---

### Task 10: Final Polish and Build Verification

**Files:**
- Various touch-ups

- [ ] **Step 1: Run full build**

```bash
npm run build
```

Fix any TypeScript errors that arise from the component rewrites.

- [ ] **Step 2: Visual review on dev server**

Navigate through every public page:
1. Landing page — all sections coherent
2. Events listing → event detail → registration dialog
3. About page — all member tabs
4. Magazine page — search and filter
5. Resources page — category filtering

Check at 375px (mobile) and 1440px (desktop).

- [ ] **Step 3: Check accessibility**

- Text contrast: all text readable against dark backgrounds (4.5:1 minimum)
- Focus states: visible focus rings on all interactive elements
- Keyboard navigation: tab through all pages
- Reduced motion: verify `prefers-reduced-motion` is respected

- [ ] **Step 4: Commit any remaining fixes**

```bash
git add -A
git commit -m "fix: final polish and accessibility adjustments for dark glass UI"
```
